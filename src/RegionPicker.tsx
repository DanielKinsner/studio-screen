export type Region = { x: number; y: number; width: number; height: number };
export default function RegionPicker({ enabled, region, thumbnail, onToggle, onChange }: { enabled: boolean; region: Region; thumbnail?: string; onToggle: (v: boolean) => void; onChange: (v: Region) => void }) {
  const update = (key: keyof Region, value: number) => { const next = { ...region, [key]: value }; next.width = Math.max(.05, Math.min(next.width, 1 - next.x)); next.height = Math.max(.05, Math.min(next.height, 1 - next.y)); onChange(next); };
  return <div className="region-picker"><div className="select-field"><span>Capture area</span><select aria-label="Capture area" value={enabled ? 'region' : 'full'} onChange={e => onToggle(e.target.value === 'region')}><option value="full">Entire source</option><option value="region">Custom area</option></select></div>{enabled && <><div className="region-preview" style={thumbnail ? { backgroundImage: `url(${thumbnail})` } : {}}><div style={{ left: `${region.x * 100}%`, top: `${region.y * 100}%`, width: `${region.width * 100}%`, height: `${region.height * 100}%` }}><span>Recorded area</span></div></div><div className="region-fields">{(['x', 'y', 'width', 'height'] as const).map(key => <label key={key}>{({ x: 'Left', y: 'Top', width: 'Width', height: 'Height' })[key]} %<input aria-label={`Capture area ${key}`} type="number" min={key === 'x' || key === 'y' ? 0 : 5} max={key === 'x' || key === 'y' ? 95 : 100} step={1} value={Math.round(region[key] * 100)} onChange={e => update(key, Math.max(key === 'x' || key === 'y' ? 0 : .05, Math.min(key === 'x' || key === 'y' ? .95 : 1, +e.target.value / 100)))} /></label>)}</div><p className="helper-text">Only this area is stored in the recording. Percentages are relative to your selected screen or window.</p></>}</div>;
}
