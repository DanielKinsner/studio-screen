import { useCallback, useEffect, useRef, useState } from 'react';
import type { ReactNode } from 'react';
import { ArrowDownToLine, ArrowLeft, ArrowRight, AudioLines, Check, CheckCheck, ChevronDown, ChevronRight, Circle, CircleHelp, Clapperboard, Copy, Download, Expand, FileVideo, FolderOpen, ImagePlus, Layers, LoaderCircle, Maximize, Mic, Monitor, MousePointer2, Move, Music2, Pause, Play, Plus, Redo2, Scissors, Settings2, SlidersHorizontal, Sparkles, Square, Subtitles, Trash2, Type, Undo2, Upload, Volume2, WandSparkles, X, ZoomIn } from 'lucide-react';
import { backgrounds, defaults, newProject } from './types';
import RegionPicker from './RegionPicker';
import Filmstrip from './Filmstrip';
import type { DesktopSource, Project, Settings } from './types';
import { autoZooms, clamp, outputDuration, parseSrt, timecode } from './timeline';
import { dimensions, renderFrame } from './compositor';
import { capture, exportVideo, getDuration, loadVideo, releaseVideo, videoMime } from './media';
import { deleteProject, download, listProjects, projectFile, readProject, saveProject } from './storage';

const tabs = [{ id: 'background', label: 'Canvas', icon: Layers }, { id: 'zoom', label: 'Zoom', icon: ZoomIn }, { id: 'cursor', label: 'Cursor', icon: MousePointer2 }, { id: 'audio', label: 'Audio', icon: AudioLines }, { id: 'captions', label: 'Captions', icon: Subtitles }, { id: 'annotations', label: 'Annotate', icon: Type }];
const uid = () => crypto.randomUUID();
function IconButton({ label, children, onClick, disabled, active }: { label: string; children: ReactNode; onClick?: () => void; disabled?: boolean; active?: boolean }) { return <button className={`icon-button ${active ? 'active' : ''}`} title={label} aria-label={label} onClick={onClick} disabled={disabled}>{children}</button>; }
function Toggle({ label, checked, onChange, description }: { label: string; checked: boolean; onChange: (v: boolean) => void; description?: string }) { return <label className="toggle-row"><span>{label}{description && <small>{description}</small>}</span><button role="switch" aria-label={label} aria-checked={checked} className={`switch ${checked ? 'on' : ''}`} onClick={() => onChange(!checked)}><span /></button></label>; }
function Slider({ label, value, min = 0, max = 100, step = 1, unit = '', onChange }: { label: string; value: number; min?: number; max?: number; step?: number; unit?: string; onChange: (v: number) => void }) { return <label className="slider-field"><span>{label}<output>{Number(value.toFixed(2))}{unit}</output></span><input aria-label={label} type="range" min={min} max={max} step={step} value={value} onChange={e => onChange(+e.target.value)} style={{ '--fill': `${(value - min) / (max - min) * 100}%` } as React.CSSProperties} /></label>; }
function Modal({ title, subtitle, children, onClose, wide }: { title: string; subtitle?: string; children: ReactNode; onClose: () => void; wide?: boolean }) {
  const ref = useRef<HTMLDivElement>(null);
  useEffect(() => { const previous = document.activeElement as HTMLElement; ref.current?.focus(); const onKey = (e: KeyboardEvent) => { if (e.key === 'Escape') onClose(); if (e.key === 'Tab') { const nodes = ref.current?.querySelectorAll<HTMLElement>('button:not(:disabled), input, select, textarea, [tabindex="0"]'); if (!nodes?.length) return; const first = nodes[0], last = nodes[nodes.length - 1]; if (e.shiftKey && (document.activeElement === first || document.activeElement === ref.current)) { e.preventDefault(); last.focus(); } else if (!e.shiftKey && document.activeElement === last) { e.preventDefault(); first.focus(); } } }; document.addEventListener('keydown', onKey); return () => { document.removeEventListener('keydown', onKey); previous?.focus(); }; }, [onClose]);
  return <div className="modal-backdrop" onMouseDown={e => { if (e.target === e.currentTarget) onClose(); }}><div className={`modal ${wide ? 'wide' : ''}`} role="dialog" aria-modal="true" aria-label={title} tabIndex={-1} ref={ref}><div className="modal-heading"><div><h2>{title}</h2>{subtitle && <p>{subtitle}</p>}</div><IconButton label="Close dialog" onClick={onClose}><X size={20} /></IconButton></div>{children}</div></div>;
}
export default function App() {
  const [project, setProject] = useState<Project>(() => newProject());
  const [ready, setReady] = useState(false); const [saved, setSaved] = useState('Saving…');
  const [tab, setTab] = useState('background'); const [backgroundTab, setBackgroundTab] = useState('Gradient');
  const [time, setTime] = useState(2); const [playing, setPlaying] = useState(false); const [selected, setSelected] = useState<string | null>(null);
  const [modal, setModal] = useState<'record' | 'export' | 'library' | 'help' | null>(null);
  const [toast, setToast] = useState(''); const [busy, setBusy] = useState(false);
  const [projects, setProjects] = useState<Project[]>([]); const [sources, setSources] = useState<DesktopSource[]>([]); const [source, setSource] = useState('');
  const [regionEnabled, setRegionEnabled] = useState(false); const [region, setRegion] = useState({ x: .1, y: .1, width: .8, height: .8 });
  const [mic, setMic] = useState(false); const [systemAudio, setSystemAudio] = useState(true); const [denoise, setDenoise] = useState(true); const [recordFps, setRecordFps] = useState(30);
  const [recording, setRecording] = useState(false); const [recordPaused, setRecordPaused] = useState(false); const [recordTime, setRecordTime] = useState(0); const [notes, setNotes] = useState(''); const [showNotes, setShowNotes] = useState(false);
  const [exportFormat, setExportFormat] = useState('webm'); const [exportHeight, setExportHeight] = useState(1080); const [exportFps, setExportFps] = useState(30); const [exportProgress, setExportProgress] = useState<number | null>(null); const [exportDone, setExportDone] = useState(false);
  const [mediaVersion, setMediaVersion] = useState(0); const [timelineScale, setTimelineScale] = useState(1);
  const canvas = useRef<HTMLCanvasElement>(null); const video = useRef<HTMLVideoElement | null>(null); const music = useRef<HTMLVideoElement | null>(null); const background = useRef<HTMLImageElement | null>(null);
  const captureRef = useRef<Awaited<ReturnType<typeof capture>> | null>(null); const abortRef = useRef<AbortController | null>(null);
  const fileInput = useRef<HTMLInputElement>(null), imageInput = useRef<HTMLInputElement>(null), audioInput = useRef<HTMLInputElement>(null), captionInput = useRef<HTMLInputElement>(null);
  const history = useRef<Project[]>([]), future = useRef<Project[]>([]); const stateRef = useRef(project); stateRef.current = project;
  const tRef = useRef(time); tRef.current = time;
  const s = project.settings; const duration = outputDuration(project); const zooms = autoZooms(project);
  const notify = useCallback((value: string) => setToast(value), []);
  const edit = useCallback((fn: (p: Project) => Project) => { const previous = stateRef.current; const next = fn(previous); history.current = [...history.current.slice(-39), previous]; future.current = []; stateRef.current = next; setProject(next); }, []);
  const setting = <K extends keyof Settings>(key: K, value: Settings[K]) => edit(p => ({ ...p, settings: { ...p.settings, [key]: value } }));
  const undo = useCallback(() => { const previous = history.current.pop(); if (previous) { future.current.push(stateRef.current); stateRef.current = previous; setProject(previous); } }, []);
  const redo = useCallback(() => { const next = future.current.pop(); if (next) { history.current.push(stateRef.current); stateRef.current = next; setProject(next); } }, []);
  const closeModal = useCallback(() => { if (abortRef.current) return; setModal(null); }, []);
  const openProject = (p: Project) => { setPlaying(false); history.current = []; future.current = []; setSelected(null); setProject(p); setTime(p.trimStart); setModal(null); };
  useEffect(() => { let alive = true; listProjects().then(items => { if (!alive) return; const latest = items.sort((a, b) => b.updated - a.updated)[0]; if (latest) { setProject(latest); setTime(latest.trimStart); } }).catch(() => notify('Local storage is unavailable. Download your project to keep a copy.')).finally(() => { if (alive) setReady(true); }); return () => { alive = false; }; }, [notify]);
  useEffect(() => { if (!ready) return; setSaved('Saving…'); const timer = setTimeout(() => { saveProject(project).then(() => setSaved('Saved locally')).catch(() => { setSaved('Not saved'); notify('Your browser storage is full or unavailable. Download the project to keep your work.'); }); }, 700); return () => clearTimeout(timer); }, [project, ready, notify]);
  useEffect(() => { if (!toast) return; const timer = setTimeout(() => setToast(''), 6000); return () => clearTimeout(timer); }, [toast]);
  useEffect(() => {
    let alive = true; let loaded: HTMLVideoElement | null = null; setPlaying(false); video.current = null;
    if (project.video) loadVideo(project.video).then(v => { loaded = v; if (!alive) return releaseVideo(v); video.current = v; v.currentTime = stateRef.current.trimStart; setMediaVersion(n => n + 1); }).catch(e => notify(e.message));
    return () => { alive = false; releaseVideo(loaded); if (video.current === loaded) video.current = null; };
  }, [project.video, notify]);
  useEffect(() => { let alive = true; let loaded: HTMLVideoElement | null = null; music.current = null; if (project.music) loadVideo(project.music).then(v => { loaded = v; if (!alive) return releaseVideo(v); v.loop = true; music.current = v; }).catch(e => notify(e.message)); return () => { alive = false; releaseVideo(loaded); }; }, [project.music, notify]);
  useEffect(() => { background.current = null; if (!project.backgroundImage) return; const img = new Image(); const url = URL.createObjectURL(project.backgroundImage); img.src = url; img.onload = () => { background.current = img; setMediaVersion(n => n + 1); }; return () => { img.onload = null; URL.revokeObjectURL(url); }; }, [project.backgroundImage]);
  useEffect(() => { if (!canvas.current) return; Object.assign(canvas.current, dimensions(s.aspect, 900)); renderFrame(canvas.current, project, time, { video: video.current, background: background.current }); }, [project, time, mediaVersion, s.aspect]);
  useEffect(() => { const v = video.current; if (!playing && v && Math.abs(v.currentTime - time) > .04) { v.currentTime = time; v.onseeked = () => setMediaVersion(n => n + 1); } }, [time, playing, mediaVersion]);
  useEffect(() => {
    if (!playing) { video.current?.pause(); music.current?.pause(); return; }
    if (tRef.current >= stateRef.current.trimEnd - .05) { tRef.current = stateRef.current.trimStart; setTime(tRef.current); }
    let frame = 0, last = performance.now();
    const tick = (now: number) => {
      const p = stateRef.current; let next = tRef.current + (now - last) / 1000 * p.settings.speed; last = now;
      for (const cut of [...p.cuts].sort((a, b) => a.start - b.start)) if (next >= cut.start && next < cut.end) next = cut.end;
      if (next >= p.trimEnd) { setTime(p.trimEnd); setPlaying(false); return; }
      if (video.current && Math.abs(video.current.currentTime - next) > .2) video.current.currentTime = next;
      tRef.current = next; setTime(next); frame = requestAnimationFrame(tick);
    };
    const v = video.current; if (v) { v.currentTime = tRef.current; v.playbackRate = s.speed; v.volume = s.volume / 100; void v.play().catch(() => setPlaying(false)); }
    if (music.current) { music.current.volume = s.musicVolume / 100; void music.current.play().catch(() => {}); }
    frame = requestAnimationFrame(tick); return () => cancelAnimationFrame(frame);
  }, [playing, s.speed, s.volume, s.musicVolume]);
  const removeSelected = useCallback(() => { if (!selected) return; edit(p => ({ ...p, zooms: p.zooms.filter(z => z.id !== selected), captions: p.captions.filter(c => c.id !== selected), annotations: p.annotations.filter(a => a.id !== selected), cuts: p.cuts.filter(c => c.id !== selected) })); setSelected(null); }, [selected, edit]);
  useEffect(() => { const key = (e: KeyboardEvent) => { if ((e.target as HTMLElement).closest('input,textarea,select,[contenteditable="true"]') || modal || recording) return;
    if (e.code === 'Space') { e.preventDefault(); setPlaying(v => !v); }
    if ((e.ctrlKey || e.metaKey) && e.key.toLowerCase() === 'z') { e.preventDefault(); e.shiftKey ? redo() : undo(); }
    if (e.key === 'ArrowLeft' || e.key === 'ArrowRight') { e.preventDefault(); setPlaying(false); setTime(t => clamp(t + (e.key === 'ArrowRight' ? 1 : -1) * (e.shiftKey ? 1 : 1 / 30), project.trimStart, project.trimEnd)); }
    if (e.key === 'Delete' || e.key === 'Backspace') removeSelected();
  }; window.addEventListener('keydown', key); return () => window.removeEventListener('keydown', key); }, [modal, recording, redo, undo, project.trimStart, project.trimEnd, removeSelected]);
  useEffect(() => window.studioDesktop?.onStop(() => captureRef.current?.stop()), []);
  useEffect(() => { if (!recording) return; const timer = setInterval(() => setRecordTime(captureRef.current?.elapsed() || 0), 200); return () => clearInterval(timer); }, [recording]);
  useEffect(() => { const prevent = (e: BeforeUnloadEvent) => { if (recording || exportProgress !== null || saved !== 'Saved locally') { e.preventDefault(); e.returnValue = ''; } }; window.addEventListener('beforeunload', prevent); return () => window.removeEventListener('beforeunload', prevent); }, [recording, exportProgress, saved]);
  async function importFile(file?: File) {
    if (!file) return; setBusy(true); setPlaying(false);
    try {
      if (file.name.endsWith('.studio')) { openProject(await readProject(file)); notify('Project opened. All edits and media restored.'); }
      else { const v = await loadVideo(file); let d: number; try { d = await getDuration(v); } finally { releaseVideo(v); } if (!d || !Number.isFinite(d)) throw new Error('Could not read the video duration.'); const p = newProject(false); p.name = file.name.replace(/\.[^.]+$/, ''); p.video = file; p.duration = d; p.trimEnd = d; openProject(p); notify('Video imported. Add a zoom to choose your focus points.'); }
    } catch (e) { notify((e as Error).message); } finally { setBusy(false); if (fileInput.current) fileInput.current.value = ''; }
  }
  async function openRecord() { setPlaying(false); setModal('record'); if (window.studioDesktop) { try { const items = await window.studioDesktop.sources(); setSources(items); setSource(items[0]?.id || ''); } catch (e) { notify((e as Error).message); } } }
  async function startRecord() {
    setBusy(true);
    try {
      if (!navigator.mediaDevices?.getDisplayMedia) throw new Error('Screen capture needs the desktop app or Chrome/Edge on localhost or HTTPS.');
      if (window.studioDesktop) { if (!source) throw new Error('Choose a screen or window first.'); await window.studioDesktop.selectSource(source); }
      const control = await capture({ mic, camera: false, system: systemAudio, denoise, fps: recordFps, region: regionEnabled ? region : undefined }); captureRef.current = control; setModal(null); setRecording(true); setRecordPaused(false); setRecordTime(0); setBusy(false);
      const result = await control.done; setRecording(false); captureRef.current = null;
      if (result.duration < .2 || !result.video.size) throw new Error('The recording was too short. Record for at least a second.');
      const p = newProject(false); p.name = `Recording · ${new Date().toLocaleDateString(undefined, { month: 'short', day: 'numeric' })}`; p.video = result.video; p.duration = result.duration; p.trimEnd = result.duration; p.points = result.points; openProject(p);
      notify(systemAudio && !result.hasSystemAudio ? 'Recording ready. The selected source did not provide system audio.' : 'Recording ready. Make it your own.');
    } catch (e) { setRecording(false); notify((e as Error).name === 'NotAllowedError' ? 'Capture cancelled or permission denied. You can try again or import a video.' : (e as Error).message); } finally { setBusy(false); }
  }
  function addZoom() { const start = clamp(time, project.trimStart, project.trimEnd - .3); const id = uid(); edit(p => ({ ...p, zooms: [...p.zooms, { id, start, end: Math.min(start + 3, p.trimEnd), x: .5, y: .5, scale: 1.8 }] })); setSelected(id); setTab('zoom'); notify('Zoom added. Click the preview to set its focus point.'); }
  function addCut() { const start = Math.min(time, project.trimEnd - .15), end = Math.min(start + 1, project.trimEnd); if (duration <= (end - start) / s.speed + .1) { notify('Keep at least a little footage in your project.'); return; } const id = uid(); edit(p => ({ ...p, cuts: [...p.cuts, { id, start, end }] })); setSelected(id); notify('Removed one second from this point. Undo to restore it, or adjust the cut below.'); }
  async function runExport() {
    setPlaying(false); setExportDone(false); const controller = new AbortController(); abortRef.current = controller; setExportProgress(0);
    try { const blob = await exportVideo(project, { format: exportFormat, height: exportFormat === 'gif' ? Math.min(exportHeight, 480) : exportHeight, fps: exportFormat === 'gif' ? Math.min(exportFps, 15) : exportFps, signal: controller.signal, progress: setExportProgress }); download(blob, `${project.name.replace(/[^a-z0-9 -]/gi, '').trim() || 'Studio Screen'}.${exportFormat}`); setExportDone(true); notify('Export complete. Your file is ready in downloads.'); } catch (e) { notify((e as Error).name === 'AbortError' ? 'Export cancelled. Your project is unchanged.' : (e as Error).message); } finally { abortRef.current = null; setExportProgress(null); setMediaVersion(n => n + 1); }
  }
  const chosenZoom = project.zooms.find(z => z.id === selected), chosenAnnotation = project.annotations.find(a => a.id === selected), chosenCut = project.cuts.find(c => c.id === selected);
  const changeZoom = (key: string, value: number) => edit(p => ({ ...p, zooms: p.zooms.map(z => z.id === selected ? { ...z, [key]: value } : z) }));
  const scrub = (e: React.PointerEvent<HTMLDivElement>) => { const rect = e.currentTarget.getBoundingClientRect(); const next = clamp((e.clientX - rect.left) / rect.width * project.duration, project.trimStart, project.trimEnd); setPlaying(false); setTime(next); if (e.type === 'pointerdown') e.currentTarget.setPointerCapture(e.pointerId); };
  return <div className="app-shell" onDragOver={e => e.preventDefault()} onDrop={e => { e.preventDefault(); if (!recording && !busy && exportProgress === null) void importFile(e.dataTransfer.files[0]); }}>
    <header className="topbar"><button className="brand" onClick={async () => { setProjects((await listProjects()).sort((a, b) => b.updated - a.updated)); setModal('library'); }} aria-label="Open project library"><span className="brand-mark"><span /><span /></span><span>studio<span className="brand-light">screen</span></span><ChevronDown size={13} /></button><div className="header-divider" /><div className="project-name"><input aria-label="Project name" value={project.name} onChange={e => edit(p => ({ ...p, name: e.target.value }))} /><span className="save-status"><CheckCheck size={12} />{saved}</span></div><div className="topbar-actions"><button className="button subtle" onClick={() => fileInput.current?.click()} disabled={busy || recording}><Upload size={15} /><span>Import</span></button><button className="button record-button" onClick={openRecord} disabled={busy || recording}><Circle size={12} fill="currentColor" />New recording</button><button className="button primary" onClick={() => { setExportDone(false); setModal('export'); }} disabled={recording}><ArrowDownToLine size={16} />Export video<ChevronDown size={13} /></button></div></header>
    <main className="workspace"><nav className="tool-rail" aria-label="Editing tools">{tabs.map(item => <button key={item.id} className={`tool ${tab === item.id ? 'selected' : ''}`} onClick={() => setTab(item.id)} aria-pressed={tab === item.id}><item.icon size={20} strokeWidth={1.6} /><span>{item.label}</span></button>)}<div className="rail-bottom"><IconButton label="Keyboard shortcuts and help" onClick={() => setModal('help')}><CircleHelp size={19} /></IconButton><span className="avatar">S</span></div></nav>
    <section className="editor"><div className="editor-toolbar"><div className="project-label"><Clapperboard size={15} /><span>{project.demo ? 'The creative workspace' : 'Your recording'}</span>{project.demo && <span className="sample-tag">SAMPLE PROJECT</span>}</div><div className="editor-tools"><IconButton label="Undo (Ctrl+Z)" disabled={!history.current.length} onClick={undo}><Undo2 size={16} /></IconButton><IconButton label="Redo (Ctrl+Shift+Z)" disabled={!future.current.length} onClick={redo}><Redo2 size={16} /></IconButton><span className="vertical-line" /><button className="fit-button" onClick={() => { const el = canvas.current?.parentElement; if (document.fullscreenElement) void document.exitFullscreen(); else void el?.requestFullscreen().catch(() => notify('Fullscreen is unavailable in this window.')); }}>Fit<ChevronDown size={12} /></button></div></div>
      <div className="preview-area"><div className="preview-halo" /><div className={`preview-frame ${chosenZoom || chosenAnnotation ? 'targeting' : ''}`} style={{ aspectRatio: s.aspect.replace(':', '/') }} onClick={e => { const r = e.currentTarget.getBoundingClientRect(); const x = clamp((e.clientX - r.left) / r.width, 0, 1), y = clamp((e.clientY - r.top) / r.height, 0, 1); if (chosenZoom) { edit(p => ({ ...p, zooms: p.zooms.map(z => z.id === selected ? { ...z, x, y } : z) })); } else if (chosenAnnotation) edit(p => ({ ...p, annotations: p.annotations.map(a => a.id === selected ? { ...a, x, y } : a) })); }}><canvas ref={canvas} aria-label="Composited video preview" />{busy && !recording && <div className="preview-loading"><LoaderCircle className="spin" size={24} /><span>Preparing your media…</span></div>}</div><div className="preview-meta"><span><span className="tiny-dot" />{project.demo ? 'Made for the way you create' : `${project.points.length ? 'Cursor tracking captured' : 'Original capture'} · ${timecode(project.duration)}`}</span><button onClick={() => { setting('aspect', s.aspect === '16:9' ? '9:16' : '16:9'); }}><Maximize size={12} />{s.aspect}<ChevronDown size={11} /></button></div></div>
      <div className="playback-bar"><span className="playback-hint"><Sparkles size={13} />A good take. A great story.</span><div className="transport"><IconButton label="Back one second" onClick={() => { setPlaying(false); setTime(t => Math.max(project.trimStart, t - 1)); }}><ArrowLeft size={15} /></IconButton><button className="play-button" aria-label={playing ? 'Pause playback' : 'Play video'} onClick={() => setPlaying(v => !v)}>{playing ? <Pause size={16} fill="currentColor" /> : <Play size={16} fill="currentColor" />}</button><IconButton label="Forward one second" onClick={() => { setPlaying(false); setTime(t => Math.min(project.trimEnd, t + 1)); }}><ArrowRight size={15} /></IconButton><span className="time-display">{timecode(time)}<span>/ {timecode(project.trimEnd)}</span></span></div><div className="playback-right"><select aria-label="Playback speed" value={s.speed} onChange={e => setting('speed', +e.target.value)}>{[.5, .75, 1, 1.25, 1.5, 2, 3, 4].map(n => <option key={n} value={n}>{n}×</option>)}</select><IconButton label="Preview fullscreen" onClick={() => void canvas.current?.parentElement?.requestFullscreen().catch(() => notify('Fullscreen is unavailable.'))}><Expand size={15} /></IconButton></div></div>
      <section className="timeline-section" aria-label="Video timeline"><div className="timeline-toolbar"><div><button className="timeline-button" onClick={addCut}><Scissors size={15} />Cut</button><button className="timeline-button" onClick={addZoom}><ZoomIn size={15} />Add zoom</button><span className="vertical-line" /><IconButton label="Delete selected edit" disabled={!selected || selected.startsWith('auto-')} onClick={removeSelected}><Trash2 size={14} /></IconButton></div><div><span className="timeline-duration">{timecode(duration)} duration</span><span className="timeline-zoom-label">−</span><input className="timeline-scale" aria-label="Timeline zoom" type="range" min={1} max={4} step={.25} value={timelineScale} onChange={e => setTimelineScale(+e.target.value)} /><span className="timeline-zoom-label">+</span></div></div><div className="timeline-body"><div className="track-labels"><div /><span><Monitor size={14} />Screen</span><span><ZoomIn size={14} />Zoom</span><span><Subtitles size={14} />Captions</span><span><Layers size={14} />Elements</span></div><div className="tracks-scroll"><div className="tracks" style={{ width: `${timelineScale * 100}%` }} onPointerDown={scrub} onPointerMove={e => { if (e.buttons === 1 && e.currentTarget.hasPointerCapture(e.pointerId)) scrub(e); }}><div className="time-ruler">{Array.from({ length: 9 }, (_, i) => <span key={i} style={{ left: `${i / 8 * 100}%` }}>{timecode(project.duration * i / 8)}</span>)}</div><div className="screen-track"><div className="screen-clip" style={{ left: `${project.trimStart / project.duration * 100}%`, width: `${(project.trimEnd - project.trimStart) / project.duration * 100}%` }}><span className="clip-grip">Ⅱ</span><Filmstrip project={project} /><div className="clip-title"><FileVideo size={12} />{project.demo ? 'Creative workspace · sample' : project.name}</div><span className="clip-grip end">Ⅱ</span></div>{project.cuts.map(c => <button title={`Cut ${timecode(c.start)} to ${timecode(c.end)}`} key={c.id} className={`cut-region ${selected === c.id ? 'selected' : ''}`} style={{ left: `${c.start / project.duration * 100}%`, width: `${(c.end - c.start) / project.duration * 100}%` }} onPointerDown={e => e.stopPropagation()} onClick={() => setSelected(c.id)}><Scissors size={12} /></button>)}</div><div className="zoom-track">{zooms.map(z => <button key={z.id} className={`zoom-clip ${selected === z.id ? 'selected' : ''}`} style={{ left: `${z.start / project.duration * 100}%`, width: `${(z.end - z.start) / project.duration * 100}%` }} onPointerDown={e => e.stopPropagation()} onClick={() => { setSelected(z.id); setTab('zoom'); setTime((z.start + z.end) / 2); setPlaying(false); }}><ZoomIn size={11} /><span>{z.id.startsWith('auto-') ? 'Auto zoom' : `${z.scale.toFixed(1)}× zoom`}</span></button>)}</div><div className="caption-track">{project.captions.map(c => <button key={c.id} className={`caption-clip ${selected === c.id ? 'selected' : ''}`} style={{ left: `${c.start / project.duration * 100}%`, width: `${(Math.min(c.end, project.duration) - c.start) / project.duration * 100}%` }} onPointerDown={e => e.stopPropagation()} onClick={() => { setSelected(c.id); setTab('captions'); setTime(c.start); }}>{c.text}</button>)}</div><div className="annotation-track">{project.annotations.map(a => <button key={a.id} className={`annotation-clip ${selected === a.id ? 'selected' : ''}`} style={{ left: `${a.start / project.duration * 100}%`, width: `${(a.end - a.start) / project.duration * 100}%` }} onPointerDown={e => e.stopPropagation()} onClick={() => { setSelected(a.id); setTab('annotations'); setTime(a.start); }}>{a.type === 'blur' ? 'Redact' : a.type}</button>)}</div><div className="playhead" style={{ left: `${time / project.duration * 100}%` }}><span /></div></div></div></div><div className="timeline-footer"><div className="trim-inputs"><Scissors size={12} /><label>In <input aria-label="Trim start in seconds" type="number" min={0} max={project.trimEnd - .1} step={.1} value={Number(project.trimStart.toFixed(2))} onChange={e => { const start = clamp(+e.target.value, 0, project.trimEnd - .1); edit(p => ({ ...p, trimStart: start })); setTime(start); }} /></label><label>Out <input aria-label="Trim end in seconds" type="number" min={project.trimStart + .1} max={project.duration} step={.1} value={Number(project.trimEnd.toFixed(2))} onChange={e => edit(p => ({ ...p, trimEnd: clamp(+e.target.value, p.trimStart + .1, p.duration) }))} /></label>{chosenCut && <label>Cut ends <input aria-label="Cut end in seconds" type="number" step={.1} value={Number(chosenCut.end.toFixed(2))} onChange={e => edit(p => ({ ...p, cuts: p.cuts.map(c => c.id === selected ? { ...c, end: clamp(+e.target.value, c.start + .1, p.trimEnd) } : c) }))} /></label>}</div><span><kbd>space</kbd> to play / pause</span></div></section>
    </section>
    <aside className="inspector"><div className="inspector-heading"><h1>{tabs.find(t => t.id === tab)?.label === 'Canvas' ? 'Make it yours' : tabs.find(t => t.id === tab)?.label}</h1><SlidersHorizontal size={16} /></div><div className="inspector-content">
      {tab === 'background' && <><div className="section-title"><h2>Background</h2><span className="mini-label">STYLE</span></div><div className="segmented">{['Gradient', 'Solid', 'Image'].map(t => <button className={backgroundTab === t ? 'active' : ''} key={t} onClick={() => { setBackgroundTab(t); if (t === 'Solid') setting('background', 'solid'); if (t === 'Gradient' && !backgrounds.some(b => b.id === s.background)) setting('background', 'dune'); if (t === 'Image' && project.backgroundImage) setting('background', 'image'); }}>{t}</button>)}</div>{backgroundTab === 'Gradient' && <div className="background-grid">{backgrounds.map(b => <button key={b.id} className={`background-swatch ${s.background === b.id ? 'selected' : ''}`} style={{ background: `linear-gradient(135deg, ${b.colors.join(',')})` }} title={b.name} aria-label={`${b.name} background`} onClick={() => setting('background', b.id)}>{s.background === b.id && <Check size={17} />}<span>{b.name}</span></button>)}</div>}{backgroundTab === 'Solid' && <label className="color-control"><input aria-label="Background color" type="color" value={s.color} onChange={e => setting('color', e.target.value)} /><span>{s.color.toUpperCase()}</span></label>}{backgroundTab === 'Image' && <button className="upload-area" onClick={() => imageInput.current?.click()}><ImagePlus size={23} /><span>{project.backgroundImage ? 'Replace background image' : 'Choose a background image'}</span><small>JPG, PNG or WebP · stays on this device</small></button>}<div className="preset-caption"><span>{backgrounds.find(b => b.id === s.background)?.name || (s.background === 'solid' ? 'Custom color' : 'Custom image')}</span><span>Made to stand out</span></div><div className="section-divider" /><div className="section-title"><h2>Composition</h2><button className="text-button" onClick={() => edit(p => ({ ...p, settings: { ...p.settings, padding: defaults.padding, radius: defaults.radius, shadow: defaults.shadow, crop: 0, aspect: '16:9' } }))}>Reset</button></div><label className="select-field"><span>Aspect ratio</span><select aria-label="Aspect ratio" value={s.aspect} onChange={e => setting('aspect', e.target.value)}><option value="16:9">16:9 · Landscape</option><option value="9:16">9:16 · Portrait</option><option value="1:1">1:1 · Square</option><option value="4:3">4:3 · Classic</option><option value="4:5">4:5 · Social</option><option value="21:9">21:9 · Cinema</option></select></label><Slider label="Padding" value={s.padding} max={25} unit="%" onChange={v => setting('padding', v)} /><Slider label="Roundness" value={s.radius} max={40} unit=" px" onChange={v => setting('radius', v)} /><Slider label="Shadow" value={s.shadow} unit="%" onChange={v => setting('shadow', v)} /><Slider label="Crop to fill" value={s.crop} max={100} unit="%" onChange={v => setting('crop', v)} /><div className="inspector-note"><Sparkles size={16} /><p>A little framing goes a long way.<br /><span>Your screen. With a little more you.</span></p></div></>}
      {tab === 'zoom' && <><Toggle label="Automatic zoom" checked={s.autoZoom} onChange={v => setting('autoZoom', v)} description="Smooth focus around captured clicks" /><Slider label="Zoom strength" value={s.zoomStrength} min={1.1} max={3} step={.05} unit="×" onChange={v => setting('zoomStrength', v)} /><p className="helper-text">{project.demo ? 'This sample includes two example click events.' : project.points.length ? 'Focus points come from your desktop recording.' : 'This clip has no click metadata. Add manual zooms to guide attention.'}</p><button className="button full-width" onClick={addZoom}><Plus size={15} />Add zoom at playhead</button><div className="section-divider" />{chosenZoom ? <><div className="section-title"><h2>Selected zoom</h2><IconButton label="Remove zoom" onClick={removeSelected}><Trash2 size={14} /></IconButton></div><Slider label="Magnification" value={chosenZoom.scale} min={1.1} max={4} step={.05} unit="×" onChange={v => changeZoom('scale', v)} /><Slider label="Focus X" value={chosenZoom.x * 100} unit="%" onChange={v => changeZoom('x', v / 100)} /><Slider label="Focus Y" value={chosenZoom.y * 100} unit="%" onChange={v => changeZoom('y', v / 100)} /><div className="two-fields"><label>Start (s)<input type="number" step={.1} value={+chosenZoom.start.toFixed(2)} onChange={e => changeZoom('start', clamp(+e.target.value, 0, chosenZoom.end - .2))} /></label><label>End (s)<input type="number" step={.1} value={+chosenZoom.end.toFixed(2)} onChange={e => changeZoom('end', clamp(+e.target.value, chosenZoom.start + .2, project.duration))} /></label></div><p className="helper-text"><Move size={13} /> Click the preview to move the focus point.</p></> : <div className="empty-panel"><ZoomIn size={26} /><h3>Give the details their moment.</h3><p>Select a manual zoom on the timeline to adjust its timing and focus.</p></div>}<div className="edit-list">{zooms.map(z => <button key={z.id} className={selected === z.id ? 'active' : ''} onClick={() => { setSelected(z.id); setTime((z.start + z.end) / 2); }}><ZoomIn size={14} /><span>{z.id.startsWith('auto-') ? 'Automatic focus' : 'Manual focus'}<small>{timecode(z.start)} – {timecode(z.end)}</small></span>{z.id.startsWith('auto-') ? <Sparkles size={12} /> : <ChevronRight size={13} />}</button>)}</div></>}
      {tab === 'cursor' && <><div className="cursor-showcase"><MousePointer2 size={48} fill="#f7f1e7" stroke="#252825" strokeWidth={1} /><span>Every move, made clear.</span></div><Toggle label="Cursor overlay" checked={s.showCursor} onChange={v => setting('showCursor', v)} /><Slider label="Cursor size" value={s.cursorSize} min={.5} max={4} step={.1} unit="×" onChange={v => setting('cursorSize', v)} /><Toggle label="Click highlights" checked={s.cursorHighlight} onChange={v => setting('cursorHighlight', v)} description="A soft ring on every captured click" /><p className="helper-text">{project.demo ? 'Explore cursor styling using the sample animation.' : project.points.length ? 'Cursor positions are interpolated for smooth motion. The source video may also contain its original cursor.' : 'Cursor effects need metadata from a desktop screen recording. A cursor already in imported footage cannot be resized or removed.'}</p></>}
      {tab === 'audio' && <><div className="section-title"><h2>Recording audio</h2><Volume2 size={15} /></div><Slider label="Source volume" value={s.volume} unit="%" onChange={v => setting('volume', v)} /><p className="helper-text">{project.demo ? 'The sample project is silent. Import music to add a soundtrack.' : 'Controls the microphone and system audio captured with your screen.'}</p><div className="section-divider" /><div className="section-title"><h2>Background music</h2><Music2 size={15} /></div><button className="upload-area" onClick={() => audioInput.current?.click()}><Music2 size={24} /><span>{project.music ? 'Replace soundtrack' : 'Add your soundtrack'}</span><small>Import an MP3, WAV, M4A or Ogg file</small></button>{project.music && <><Slider label="Music volume" value={s.musicVolume} unit="%" onChange={v => setting('musicVolume', v)} /><p className="helper-text">Music loops to fill the exported video.</p><button className="button full-width" onClick={() => edit(p => ({ ...p, music: undefined }))}><Trash2 size={14} />Remove soundtrack</button></>}<div className="section-divider" /><div className="inspector-note"><Mic size={16} /><p>Clean audio starts at the source.<br /><span>Enable noise suppression when recording.</span></p></div></>}
      {tab === 'captions' && <><Toggle label="Show captions" checked={s.captions} onChange={v => setting('captions', v)} /><div className="button-pair"><button className="button" onClick={() => captionInput.current?.click()}><Upload size={14} />Import SRT</button><button className="button" onClick={() => { const id = uid(); const start = Math.min(time, project.trimEnd - .2); edit(p => ({ ...p, captions: [...p.captions, { id, start, end: Math.min(start + 3, p.trimEnd), text: 'Your words, right here.' }] })); setSelected(id); }}><Plus size={14} />Add</button></div><p className="helper-text">Write captions or import an SRT transcript. Captions are burned into your export.</p><div className="caption-list">{project.captions.map(c => <div key={c.id} className={`caption-editor ${selected === c.id ? 'selected' : ''}`} onClick={() => setSelected(c.id)}><div><button className="caption-time" onClick={() => { setTime(c.start); setPlaying(false); }}>{timecode(c.start)} — {timecode(c.end)}</button><IconButton label="Delete caption" onClick={() => edit(p => ({ ...p, captions: p.captions.filter(v => v.id !== c.id) }))}><X size={13} /></IconButton></div><textarea aria-label="Caption text" value={c.text} rows={2} onChange={e => edit(p => ({ ...p, captions: p.captions.map(v => v.id === c.id ? { ...v, text: e.target.value } : v) }))} /><div className="two-fields"><label>In<input aria-label="Caption start" type="number" min={0} step={.1} value={+c.start.toFixed(2)} onChange={e => edit(p => ({ ...p, captions: p.captions.map(v => v.id === c.id ? { ...v, start: clamp(+e.target.value, 0, c.end - .1) } : v) }))} /></label><label>Out<input aria-label="Caption end" type="number" step={.1} value={+c.end.toFixed(2)} onChange={e => edit(p => ({ ...p, captions: p.captions.map(v => v.id === c.id ? { ...v, end: clamp(+e.target.value, c.start + .1, p.duration) } : v) }))} /></label></div></div>)}</div></>}
      {tab === 'annotations' && <><div className="section-title"><h2>Say it with a little emphasis.</h2></div><div className="annotation-buttons">{[{ type: 'text', label: 'Text', icon: Type }, { type: 'arrow', label: 'Arrow', icon: ArrowRight }, { type: 'blur', label: 'Redact', icon: Square }, { type: 'spotlight', label: 'Spotlight', icon: Circle }].map(a => <button key={a.type} onClick={() => { const id = uid(); const start = Math.min(time, project.trimEnd - .2); edit(p => ({ ...p, annotations: [...p.annotations, { id, type: a.type as 'text', start, end: Math.min(start + 3, p.trimEnd), text: 'Look a little closer', x: .3, y: .4, width: .2, height: .12 }] })); setSelected(id); }}><a.icon size={22} /><span>{a.label}</span></button>)}</div><p className="helper-text">Add an element at the playhead, then click the preview to place it. Redact uses an opaque mask to conceal details.</p>{chosenAnnotation && <><div className="section-divider" /><div className="section-title"><h2>Selected element</h2><IconButton label="Remove element" onClick={removeSelected}><Trash2 size={14} /></IconButton></div>{chosenAnnotation.type === 'text' && <textarea className="annotation-text" aria-label="Annotation text" value={chosenAnnotation.text} onChange={e => edit(p => ({ ...p, annotations: p.annotations.map(a => a.id === selected ? { ...a, text: e.target.value } : a) }))} />}<Slider label="Width" value={chosenAnnotation.width * 100} min={5} max={80} unit="%" onChange={v => edit(p => ({ ...p, annotations: p.annotations.map(a => a.id === selected ? { ...a, width: v / 100 } : a) }))} /><Slider label="Height" value={chosenAnnotation.height * 100} min={5} max={80} unit="%" onChange={v => edit(p => ({ ...p, annotations: p.annotations.map(a => a.id === selected ? { ...a, height: v / 100 } : a) }))} /><div className="two-fields"><label>In (s)<input type="number" step={.1} value={+chosenAnnotation.start.toFixed(2)} onChange={e => edit(p => ({ ...p, annotations: p.annotations.map(a => a.id === selected ? { ...a, start: clamp(+e.target.value, 0, a.end - .1) } : a) }))} /></label><label>Out (s)<input type="number" step={.1} value={+chosenAnnotation.end.toFixed(2)} onChange={e => edit(p => ({ ...p, annotations: p.annotations.map(a => a.id === selected ? { ...a, end: clamp(+e.target.value, a.start + .1, p.duration) } : a) }))} /></label></div></>}</>}
    </div><div className="inspector-footer"><span className="local-dot" />Local by design.<span>Your recordings stay yours.</span></div></aside></main>
    <footer className="app-footer"><span><span className="status-dot" />{window.studioDesktop ? 'Desktop studio' : 'Local workspace'}<span className="footer-dot">·</span>{saved === 'Saved locally' ? 'All changes saved on this device' : saved}</span><span>Less editing. More creating.<span className="version">v0.1</span></span></footer>
    {toast && <div className="toast" role="status"><span>{toast}</span><IconButton label="Dismiss notification" onClick={() => setToast('')}><X size={14} /></IconButton></div>}
    {recording && <div className="recording-dock" role="status"><span className={`recording-dot ${recordPaused ? 'paused' : ''}`} /><div><strong>{recordPaused ? 'Paused' : 'Recording'}</strong><span>{timecode(recordTime)}</span></div><IconButton label={recordPaused ? 'Resume recording' : 'Pause recording'} onClick={() => { if (recordPaused) captureRef.current?.resume(); else captureRef.current?.pause(); setRecordPaused(v => !v); }}>{recordPaused ? <Play size={17} /> : <Pause size={17} />}</IconButton><button className="button primary" onClick={() => captureRef.current?.stop()}><Square size={13} fill="currentColor" />Finish recording</button><IconButton label="Toggle speaker notes" onClick={() => setShowNotes(v => !v)}><Type size={17} /></IconButton>{window.studioDesktop && <kbd>Ctrl Shift R</kbd>}</div>}
    {recording && showNotes && <div className="speaker-notes"><div><strong>Speaker notes</strong><IconButton label="Hide speaker notes" onClick={() => setShowNotes(false)}><X size={14} /></IconButton></div><textarea aria-label="Live speaker notes" value={notes} onChange={e => setNotes(e.target.value)} /></div>}
    {modal === 'record' && <Modal title="Your next great take." subtitle="Pick your source. Find your flow. We’ll take care of the frame." onClose={closeModal}><div className="record-source"><Monitor size={32} /><div><strong>Screen recording</strong><p>{window.studioDesktop ? 'Choose a display or window below.' : 'Choose a screen, window or browser tab in the sharing dialog.'}</p></div></div>{window.studioDesktop && <div className="source-grid">{sources.map(item => <button key={item.id} className={source === item.id ? 'selected' : ''} onClick={() => setSource(item.id)}><img src={item.thumbnail} alt="" /><span>{item.name}</span>{source === item.id && <Check size={14} />}</button>)}</div>}<RegionPicker enabled={regionEnabled} region={region} thumbnail={sources.find(s => s.id === source)?.thumbnail} onToggle={setRegionEnabled} onChange={setRegion} /><Toggle label="Microphone (optional)" checked={mic} onChange={setMic} description="Record your voice alongside your screen" /><Toggle label="System audio" checked={systemAudio} onChange={setSystemAudio} description="Availability depends on the selected source" />{mic && <Toggle label="Noise suppression" checked={denoise} onChange={setDenoise} description="Reduce background noise and echo during capture" />}<label className="select-field"><span>Frame rate</span><select aria-label="Recording frame rate" value={recordFps} onChange={e => setRecordFps(+e.target.value)}><option value={30}>30 fps</option><option value={60}>60 fps</option></select></label><details className="notes-details"><summary>Speaker notes <Type size={14} /></summary><textarea aria-label="Speaker notes" placeholder="A few words to keep you on track…" value={notes} onChange={e => setNotes(e.target.value)} /><small>Notes are visible in this app. If you record this window, they will be captured.</small></details><div className="modal-actions"><button className="button" onClick={closeModal}>Cancel</button><button className="button primary" disabled={busy} onClick={startRecord}>{busy ? <LoaderCircle className="spin" size={16} /> : <Circle size={12} fill="currentColor" />}{busy ? 'Preparing capture…' : 'Start recording'}</button></div></Modal>}
    {modal === 'export' && <Modal title={exportDone ? 'Ready for its close-up.' : 'Let’s put it out there.'} subtitle={exportDone ? 'Your video has been sent to your downloads.' : 'Beautifully framed. Entirely yours. No watermark.'} onClose={closeModal}><div className="export-summary"><span className="export-art"><Clapperboard size={30} /></span><div><strong>{project.name}</strong><span>{timecode(duration)} · {s.aspect} · {project.demo ? 'Sample project' : 'Screen recording'}</span></div><Check size={18} /></div>{exportDone ? <div className="export-success"><CheckCheck size={36} /><h3>That’s a wrap.</h3><p>Keep the editable project too, so your next version is just a few clicks away.</p></div> : <><label className="select-field"><span>Format</span><select aria-label="Export format" disabled={exportProgress !== null} value={exportFormat} onChange={e => setExportFormat(e.target.value)}><option value="webm">WebM · high quality</option><option value="mp4" disabled={!videoMime('mp4')}>MP4{!videoMime('mp4') ? ' · unavailable in this browser' : ' · widely compatible'}</option><option value="gif">GIF · silent, looping</option></select></label><label className="select-field"><span>Resolution</span><select aria-label="Export resolution" disabled={exportProgress !== null} value={exportHeight} onChange={e => setExportHeight(+e.target.value)}><option value={720}>720p · compact</option><option value={1080}>1080p · full HD</option><option value={2160}>2160p · 4K</option></select></label><label className="select-field"><span>Frame rate</span><select aria-label="Export frame rate" disabled={exportProgress !== null} value={exportFps} onChange={e => setExportFps(+e.target.value)}><option value={30}>30 fps</option><option value={60}>60 fps</option></select></label><div className="export-info"><Monitor size={16} /><p>{exportFormat === 'gif' ? 'GIF exports use up to 480p and 15 fps to keep files manageable. Long GIFs can use significant memory.' : 'Exports render locally in real time. Keep this window open and in the foreground until your download is ready.'}</p></div></>}{exportProgress !== null && <div className="export-progress" role="progressbar" aria-label="Export progress" aria-valuenow={Math.round(exportProgress * 100)} aria-valuemin={0} aria-valuemax={100}><div><span>Rendering your story…</span><strong>{Math.round(exportProgress * 100)}%</strong></div><span><span style={{ width: `${exportProgress * 100}%` }} /></span></div>}<div className="modal-actions spread"><button className="button subtle" disabled={exportProgress !== null} onClick={async () => { download(await projectFile(project), `${project.name || 'project'}.studio`); notify('Editable project downloaded with its media.'); }}><Download size={15} />Save project</button>{exportProgress !== null ? <button className="button" onClick={() => abortRef.current?.abort()}>Cancel export</button> : <button className="button primary" onClick={runExport}><ArrowDownToLine size={16} />{exportDone ? 'Export again' : 'Export video'}</button>}</div><button className="snapshot-button" disabled={exportProgress !== null} onClick={() => canvas.current?.toBlob(blob => { if (blob) download(blob, `${project.name}-frame.png`); })}>Or save the current frame as PNG <ArrowRight size={12} /></button></Modal>}
    {modal === 'library' && <Modal title="Your creative space." subtitle="Projects live on this device. Download a project to take it with you." onClose={closeModal} wide><div className="library-actions"><button className="button primary" onClick={openRecord}><Plus size={15} />New recording</button><button className="button" onClick={() => fileInput.current?.click()}><Upload size={15} />Import video or project</button><button className="button subtle" onClick={() => openProject(newProject())}><Sparkles size={15} />Open sample</button></div><div className="project-list">{projects.map(p => <div className="project-card" key={p.id}><button onClick={() => openProject(p)}><span className="project-thumbnail" style={{ background: `linear-gradient(135deg, ${(backgrounds.find(b => b.id === p.settings.background)?.colors || [p.settings.color, p.settings.color]).join(',')})` }}><Monitor size={27} /></span><span><strong>{p.name}</strong><small>{timecode(outputDuration(p))} · {p.demo ? 'Sample' : 'Recording'} · {new Date(p.updated).toLocaleDateString()}</small></span><ChevronRight size={16} /></button><IconButton label={`Delete ${p.name}`} onClick={async () => { await deleteProject(p.id); setProjects(v => v.filter(item => item.id !== p.id)); notify('Removed from the library. An open project will be saved again when edited.'); }}><Trash2 size={16} /></IconButton></div>)}</div></Modal>}
    {modal === 'help' && <Modal title="A little help, a lot of possibility." subtitle="Everything you need to find your flow." onClose={closeModal}><div className="shortcut-list">{[['Play / pause', 'Space'], ['Previous / next frame', '← / →'], ['Skip one second', 'Shift + ← / →'], ['Undo', 'Ctrl / ⌘ + Z'], ['Redo', 'Ctrl / ⌘ + Shift + Z'], ['Remove selected edit', 'Delete'], ['Stop desktop recording', 'Ctrl + Shift + R']].map(([a, b]) => <div key={a}><span>{a}</span><kbd>{b}</kbd></div>)}</div><p className="helper-text">Drag a video or .studio project into the workspace to open it. Click a zoom or element on the timeline to edit it. Save an editable project from the export dialog to keep a portable backup.</p><div className="export-info"><FolderOpen size={18} /><p>This build supports local capture and editing. Automatic transcription, mobile capture, and cloud sharing are not available yet. Camera capture is deferred.</p></div></Modal>}
    <input ref={fileInput} type="file" hidden accept="video/*,.studio" onChange={e => void importFile(e.target.files?.[0])} />
    <input ref={imageInput} type="file" hidden accept="image/*" onChange={e => { const file = e.target.files?.[0]; if (file) edit(p => ({ ...p, backgroundImage: file, settings: { ...p.settings, background: 'image' } })); e.target.value = ''; }} />
    <input ref={audioInput} type="file" hidden accept="audio/*" onChange={e => { const file = e.target.files?.[0]; if (file) edit(p => ({ ...p, music: file })); e.target.value = ''; }} />
    <input ref={captionInput} type="file" hidden accept=".srt,.vtt" onChange={async e => { const file = e.target.files?.[0]; if (file) { const captions = parseSrt(await file.text()).filter(c => c.start < project.duration).map(c => ({ ...c, end: Math.min(c.end, project.duration) })); if (captions.length) { edit(p => ({ ...p, captions })); notify(`Imported ${captions.length} captions.`); } else notify('No valid captions were found within this video’s duration.'); } if (captionInput.current) captionInput.current.value = ''; }} />
  </div>;
}
